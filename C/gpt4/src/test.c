// 输入一个整数 n，再输入 n 个数，输出它们的平均值（保留两位小数）。
#include <stdbool.h>
#include <stdio.h>

int
main ()
{
    int n;
    int arry[n];
    while (true)
        {
            int i;
            scanf ("%d", &n);
            for (i = 0; i = 5; i++)
                {
                }
            scanf ("%d", &arry[i]);
        }
}
